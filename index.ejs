<ul>
<% for (const item of items) { %>
<li><a href="<%- item.path %>"><% if (item.title) { %>
<b><%= item.title %></b> - <%= item.subtitle %>
<% } else { %>
<b><%= item.filename %></b>
<% } %> </a></li> <% } %>
</ul>
